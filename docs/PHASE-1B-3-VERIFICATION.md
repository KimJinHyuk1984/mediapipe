# Phase 1-B-3 완료·검증 보고

검증일: 2026-09-07.

step3, tournament, extend를 작성하여 Level 1 본문 12개 섹션을 완성했습니다.
앞 9개 섹션, 실제 Python 실습 파일, base.css, tokens.css는 이번 단계에서 수정하지 않았습니다.
위젯은 추가하지 않았습니다.

## 이미지 요구 충돌과 적용한 선택

지정된 slide-027-a.webp 하단과 slide-028-a.webp 본문에는 다른 각도 계산 방식이
직접 표시되어 있습니다. 원본 그대로 게재하면 “arctan2 등장 없음” 조건을 만족할 수 없습니다.
이 충돌을 알리고 대체 도식을 추천했으며, 원본 보존과 Level 1 계산 방식 일치를 우선하여
별도 내적·역코사인 SVG 두 장으로 대체했습니다.

- slide-027-inner-product.svg: 세 점·두 벡터 도식.
- slide-028-inner-product.svg: 내적 계산 예시·판정 구간·clip 설명.
- slide-029-b.webp: 요청한 상태머신 원본 도식 사용.

원본 027·028 WebP는 변경하지 않았습니다. 대체 파일 정보는 IMAGE-MANIFEST.md에 추가했습니다.
따라서 새 이미지 3장 검증은 원본 두 장 대신 위 SVG 두 장을 포함합니다.

## 코드에 맞춘 내용

- 각도 계산은 03angle_01.py의 벡터 내적·arccos 원문을 그대로 사용했습니다.
- 판정 표는 정확한 경계로 표시했습니다: >160, 100<θ≤160, 70<θ≤100, ≤70.
- final.py 카운트 발췌에는 data-source-lines를 기록하고, 완성본은 전체를 실었습니다.
- 비교용 잘못된 카운트 예제는 실행 파일에서 가져온 코드와 구분했습니다.
- final.py는 GOOD SQUAT 문구를 카운트 조건으로 검사하지 않습니다. TOO DEEP도 셀 수 있고,
  정확히 100도에서는 DOWN으로 바뀌지 않는다는 점을 본문과 운영 안내에 명시했습니다.
- 60초 자동 종료 기능이 없어 외부 타이머와 q 종료를 안내했습니다.
  유효 자세 확인·재측정 절차를 함께 적고, 페이스에는 준비 시간이 포함됨을 설명했습니다.
- 팔벌려뛰기는 각도 함수 대신 위치·거리 비교를 사용한다고 안내했습니다.
  Level 2 각도 구현의 통일 방향은 후속 단계에서 결정하도록 유지했습니다.

## 실제 실행 검증

상위 폴더의 Python http.server에서 /mediapipe/level1/을 Chrome + Playwright로 열었습니다.
검증 스크립트는 verify-phase1b3.cjs와 verify-practice-files.py입니다.

| 항목 | 결과 |
|---|---|
| 1. 12개 섹션 콘텐츠 / TODO 0건 | 통과 |
| 2. 웹 코드와 실제 파일의 문자 일치 | 통과, 공백·주석·줄바꿈 포함 원문·발췌 블록 대조 |
| 3. Level 1 각도 방식 | 통과, arccos 원문 유지, HTML과 새 도식에 arctan2 없음 |
| 4. 새 이미지 3장 | 통과, SVG 대체 2장 + 원본 1장, 로드·alt·width·height·loading 확인 |
| 5. 다운로드 | 통과, 신규 3개와 setup 3개 실제 다운로드 후 내용 비교 |
| 6. 전체 복사 | 통과, 19개 블록의 실제 클립보드 내용 비교 |
| 7. 발표 모드 | 통과, 12개 전부 순서 이동, 코드 높이 ≤ min(42vh, 420px), 내부 스크롤 |
| 8. 375px 표·코드 | 통과, 문서 폭 증가 없음, 긴 코드 내부 가로 스크롤 |
| 9. 라이트·다크 | 통과, 카드·콜아웃 최소 대비 5.95:1 / 8.11:1 |
| 10. 진행 체크 | 통과, lecture-progress:level1에 12개 ID 모두 기록 |
| 11. Level 2 비활성 | 통과, aria-disabled=true·href 없음·클릭 후 주소 불변 |

Windows 클립보드 줄바꿈은 CRLF를 LF로 정규화했습니다.
새 도식과 토너먼트·응용 섹션의 화면 캡처를 시각 확인했습니다.
페이지 오류·HTTP 오류 0건, git diff --check 통과.
Python 실습 파일은 정적 검사만 했으며 실제 웹캠 운동 측정은 수행하지 않았습니다.

인터랙티브 위젯, 실습 파일 동작 변경, 커밋·푸시·배포는 수행하지 않았습니다.
